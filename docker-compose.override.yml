version: '3.8'
services:
  openclaw-gateway: # Ensure this matches the service name in your main docker-compose.yml
    build: .
    volumes:
      - openclaw_config:/home/node/.openclaw
      - openclaw_workspace:/home/node/.openclaw/workspace

volumes:
  openclaw_config:
  openclaw_workspace: